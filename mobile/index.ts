// Entry point para dev-client (APK prebuildeado pide index.bundle).
import { registerRootComponent } from 'expo';
import App from './App';

// Registra App como root para la app nativa.
registerRootComponent(App);
